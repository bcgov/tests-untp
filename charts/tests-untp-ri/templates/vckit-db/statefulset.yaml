apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: {{ include "tests-untp-ri.vckitDb.fullname" . }}
  labels:
    {{- include "tests-untp-ri.labels" . | nindent 4 }}
    app.kubernetes.io/component: vckit-db
spec:
  serviceName: {{ include "tests-untp-ri.vckitDb.fullname" . }}
  replicas: 1
  selector:
    matchLabels:
      {{- include "tests-untp-ri.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: vckit-db
  template:
    metadata:
      labels:
        {{- include "tests-untp-ri.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: vckit-db
    spec:
      {{- with .Values.global.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: postgres
          image: "{{ .Values.vckitDb.image.repository }}:{{ .Values.vckitDb.image.tag }}"
          imagePullPolicy: {{ .Values.vckitDb.image.pullPolicy }}
          ports:
            - name: postgres
              containerPort: 5432
          env:
            - name: POSTGRES_USER
              value: {{ .Values.vckitDb.auth.username | quote }}
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ include "tests-untp-ri.secretName" . }}
                  key: vckit-postgres-password
            - name: POSTGRES_DB
              value: {{ .Values.vckitDb.auth.database | quote }}
            - name: PGDATA
              value: /var/lib/postgresql/data/pgdata
          volumeMounts:
            - name: data
              mountPath: /var/lib/postgresql/data
          readinessProbe:
            exec:
              command: ["pg_isready", "-U", {{ .Values.vckitDb.auth.username | quote }}, "-d", {{ .Values.vckitDb.auth.database | quote }}]
            initialDelaySeconds: 10
            periodSeconds: 10
          {{- with .Values.vckitDb.resources }}
          resources:
            {{- toYaml . | nindent 12 }}
          {{- end }}
      {{- if not .Values.vckitDb.persistence.enabled }}
      volumes:
        - name: data
          emptyDir: {}
      {{- end }}
  {{- if .Values.vckitDb.persistence.enabled }}
  volumeClaimTemplates:
    - metadata:
        name: data
      spec:
        accessModes: ["ReadWriteOnce"]
        {{- if .Values.vckitDb.persistence.storageClass }}
        storageClassName: {{ .Values.vckitDb.persistence.storageClass | quote }}
        {{- end }}
        resources:
          requests:
            storage: {{ .Values.vckitDb.persistence.size }}
  {{- end }}
