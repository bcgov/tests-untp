apiVersion: v1
kind: Service
metadata:
  name: {{ include "tests-untp-ri.vckit.fullname" . }}
  labels:
    {{- include "tests-untp-ri.labels" . | nindent 4 }}
    app.kubernetes.io/component: vckit
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 3332
      targetPort: http
  selector:
    {{- include "tests-untp-ri.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: vckit
